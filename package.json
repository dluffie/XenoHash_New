{
    "name": "xenohash",
    "version": "1.0.0",
    "description": "XenoHash - Telegram Mini App Mining Simulator",
    "scripts": {
        "build": "cd server && npm install && cd ../client && npm install && npm run build",
        "start": "cd server && node server.js"
    }
}